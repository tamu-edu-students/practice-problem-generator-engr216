<% content_for :title do %>Teacher Dashboard<% end %>

<div class="min-h-screen w-full flex flex-col" style="background-color: #9aa8c5;">
  <!-- Navbar -->
  <nav class="navbar bg-black text-white px-6 fixed top-0 z-50 w-full">
    <div class="flex-1">
      <a href="/teacher_dashboard" class="btn bg-transparent text-white border-transparent hover:border-blue-700 hover:bg-blue-500 hover:text-white transition">
        Dashboard
      </a>
    </div>
    <div class="flex-none">
      <div class="dropdown dropdown-end">
        <button class="btn bg-transparent text-white border-transparent hover:border-blue-700 hover:bg-blue-500 hover:text-white transition">
          Account ▼
        </button>
        <ul class="dropdown-content menu p-2 shadow bg-black text-white rounded-box w-36">
          <li><a href="/logout" class="text-white hover:bg-blue-500">Logout</a></li>
        </ul>
      </div>
    </div>
  </nav>

  <!-- Main Content -->
  <main class="max-w-6xl mx-auto pt-24 p-4 flex flex-col items-center min-h-screen">    
    <!-- Title just below the navbar -->
    <h1 class="w-full text-center text-5xl font-bold text-gray-800 mb-8">
      Teacher Dashboard
    </h1>
    <!-- Button Container -->
    <div class="flex flex-wrap justify-center gap-6">
      <a href="<%= manage_students_path %>" class="btn bg-gray-500 hover:bg-gray-600 text-white px-6 py-3">
        Student Management
      </a>
      <a href="<%= student_history_dashboard_path %>" class="btn bg-gray-500 hover:bg-gray-600 text-white px-6 py-3">
        Student Problem History
      </a>
      <a href="<%= new_teacher_path %>" class="btn bg-blue-500 hover:bg-blue-600 text-white px-6 py-3">
        Add New Teacher
      </a>
    </div>
  </main>
</div>
