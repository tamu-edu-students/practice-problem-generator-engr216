# <% content_for :head do %>
#   <%= stylesheet_link_tag 'student_dashboard', media: 'all' %>
# <% end %>

<div class="min-h-screen w-full flex flex-col items-center justify-center bg-#1a202c;  p-6">
  <h1 class="text-5xl font-bold text-center mb-8 text-black">Howdy, Student</h1>

  <div class="w-full max-w-screen-xl grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-6 px-6">
    <% @physics_units.each do |unit| %>
      <button class="btn btn-primary text-lg font-semibold p-6 w-full h-full flex items-center justify-center shadow-lg hover:scale-105 transition-transform">
        <%= unit %>
      </button>
    <% end %>
  </div>
</div>
